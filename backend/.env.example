# Local development. In production (Render) set these in the dashboard / render.yaml.
PORT=3001
MONGO_URI=mongodb://localhost:27017/ecommerce
JWT_SECRET=replace-with-a-long-random-string

# Browser origin allowed by CORS. Local: the Vite dev server.
# Production: your GitHub Pages ORIGIN only (no path), e.g. https://motawe3a.github.io
CLIENT_URL=http://localhost:5173
